/**
 * The name of the IPC channel we use when forward main process
 * actions to the renderer process.
 */
export const REDUX_ACTION_CHANNEL = "redux-action";

/**
 * The name of the IPC channel we use to send the command ID from
 * the renderer process to the main process.
 */
export const MENU_EXEC_CHANNEL = "menu-exec";

